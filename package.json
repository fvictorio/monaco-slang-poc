{
  "name": "monaco-slang-poc",
  "version": "1.0.0",
  "description": "Monaco + Slang proof of concept",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "slang-repl": "node scripts/slang-repl.mjs",
    "slang-smoke": "node --experimental-strip-types scripts/binding-graph-smoke.ts",
    "gh-pages": "pnpm build && gh-pages -d dist --nojekyll"
  },
  "license": "ISC",
  "packageManager": "pnpm@10.24.0",
  "dependencies": {
    "@nomicfoundation/slang": "^1.3.5",
    "monaco-editor": "^0.52.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.7"
  }
}
